[package]
name = "mcp-standalone-test"
version = "0.1.0"
edition = "2024"

[workspace]

[dependencies]
matrixcode-core = { path = "../core" }
tokio = { version = "1", features = ["full"] }
env_logger = "0.11"